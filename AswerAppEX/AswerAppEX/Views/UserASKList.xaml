<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="AswerAppEX.Views.UserASKList">
    <ContentPage.Content>
        <StackLayout>
            <ContentPage.Content>



                <ScrollView >

                    <StackLayout>
                        <Label Text="AskList!"
                VerticalOptions="CenterAndExpand" 
                HorizontalOptions="CenterAndExpand" />



                        <ListView x:Name="LvlList" RowHeight="150" SeparatorVisibility="Default">
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <TextCell Text="{Binding Name}"
                                      Detail="{Binding Descripcion}"
                                      TextColor="Black" DetailColor="Aqua">

                                    </TextCell>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </StackLayout>
                </ScrollView>

            </ContentPage.Content>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>